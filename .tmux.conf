# --- TERMINAL OPTIMIZATION ---
# Enable True Color for Windows Terminal
set -as terminal-features ",xterm-256color:RGB"

# --- CORE NEOVIM-SPEED ---
set -s escape-time 0
set -g focus-events on
set -g mouse on

# --- PREFIX ---
set -g prefix C-a
unbind C-b
bind C-a send-prefix

# --- WINDOWS PATH HANDLING ---
# Ensure new panes open in the same directory (Crucial for Nvim projects)
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

# --- NAVIGATION (The "Pseudo-Vim" Way) ---
# psmux doesn't always handle the 'is_vim' shell script well, 
# so we use direct bindings.
bind-key -n C-h select-pane -L
bind-key -n C-j select-pane -D
bind-key -n C-k select-pane -U
bind-key -n C-l select-pane -R

# --- VI MODE ---
set-window-option -g mode-keys vi
# Windows Clipboard Integration (using clip.exe)
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel "clip.exe"

# --- RELOAD CONFIG ---
# Press Prefix + r to reload the configuration file
bind r source-file ~/.tmux.conf \; display-message "Config reloaded!"

# --- AESTHETICS (MODERN PILL STYLE) ---
set -g status-position top
set -g status-justify absolute-centre
set -g status-style bg=#1e1e2e,fg=#cdd6f4

# Rounded Separators:    

# Left: Session name pill (Changes to Red when Prefix is active)
set -g status-left-length 40
set -g status-left "#{?client_prefix,#[fg=#f38ba8,bg=#1e1e2e]#[bg=#f38ba8,fg=#1e1e2e,bold] 󰈸 #S #[fg=#f38ba8,bg=#1e1e2e],#[fg=#cba6f7,bg=#1e1e2e]#[bg=#cba6f7,fg=#1e1e2e,bold] 󰭦 #S #[fg=#cba6f7,bg=#1e1e2e]}"

# Right: Time and Date pills
set -g status-right-length 60
set -g status-right "#[fg=#45475a,bg=#1e1e2e]#[fg=#cdd6f4,bg=#45475a]  %H:%M #[fg=#45475a,bg=#1e1e2e] #[fg=#313244,bg=#1e1e2e]#[fg=#cdd6f4,bg=#313244] 󰃭 %d %b #[fg=#313244,bg=#1e1e2e]"

# Window status: Individual pills with spacing
set -g window-status-separator " "

# Inactive window: Subtle grey pill
set -g window-status-format "#[fg=#313244,bg=#1e1e2e]#[fg=#cdd6f4,bg=#313244] #I #W #[fg=#313244,bg=#1e1e2e]"

# Active window: Bright mauve pill
set -g window-status-current-format "#[fg=#cba6f7,bg=#1e1e2e]#[fg=#1e1e2e,bg=#cba6f7,bold] #I #W #[fg=#cba6f7,bg=#1e1e2e]"

# Activity/Bell indicators
set -g window-status-activity-style "fg=#f9e2af,bg=#313244"
set -g window-status-bell-style "fg=#f38ba8,bg=#313244"

# --- PANE BORDERS ---
set -g pane-border-style fg=#313244
set -g pane-active-border-style fg=#cba6f7

# Pane number indicator
set -g display-panes-colour #45475a
set -g display-panes-active-colour #cba6f7

# --- PANE RESIZING (Vim-style) ---
# Use Prefix + Shift + h/j/k/l to resize panes
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# --- COPY MODE STYLING ---
# Selection color (Catppuccin Mauve)
set -g mode-style "fg=#1e1e2e,bg=#cba6f7,bold"

# --- MESSAGE STYLING ---
set -g message-style bg=#cba6f7,fg=#1e1e2e,bold
